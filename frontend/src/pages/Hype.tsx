export function Hype() {
  return (
    <div>
      <div className="page-header">
        <h1 className="page-title">Hype</h1>
        <p className="page-subtitle">Pre-interview confidence boost.</p>
      </div>
      <p style={{ color: 'var(--text-muted)' }}>60-second hype reel, 3x3 sheet, warmup routine. (Job workspace only)</p>
    </div>
  );
}
